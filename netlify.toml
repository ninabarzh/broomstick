[build]
  command = "hugo --gc --minify --cleanDestinationDir && npx pagefind --site public"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.147.3"

[[redirects]]
  from = "/admin/*"
  to = "/admin/index.html"
  status = 200